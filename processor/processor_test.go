package processor_test

import (
	. "github.com/smartystreets/goconvey/convey"
	"testing"
)

func TestProcessorFactory(t *testing.T) {
	Convey("ProcessorFactory", t, func() {
		/*
		a := processor.CommandProcessorFactory{}

		turn := cmd.NewTurn(1)
		//acceleration := cmd.NewAcceleration(1)
		//connect := cmd.NewConnect("test")

		turn_cp := a.GetCommandProcessor(&turn)
		//acceleration_cp := a.GetCommandProcessor(acceleration)
		//connect_cp := a.GetCommandProcessor(connect)



		So(turn_cp, ShouldHaveSameTypeAs, processor.TurnCommandProcessor{} )
		//So(acceleration_cp, ShouldHaveSameTypeAs, )
		//So(connect_cp, ShouldHaveSameTypeAs, )
		*/
	})
}